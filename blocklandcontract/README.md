# Blockland contract

This project demonstrates erc721 nft token creation.

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat compile
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
```

## Running script to find contract address
```cmd
npx hardhat run scripts/deploy.js --network localhost
```
